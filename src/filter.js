const words = [
  "spray",
  "limit",
  "elielitete",
  "exuberant",
  "destruction",
  "present",
];

const filteredArr = words.filter((word) => {
  return word.length > 6;
});
console.log(filteredArr);
